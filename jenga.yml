version: '2'
services:
  led:
    build:
      context: ~/jengalamp/led_manager/
    privileged: true
    environment:
      - LEDSERVICE_HOST=localhost
      - LEDSERVICE_PORT=9000
  tbot:
    build:
      context: ~/jengalamp/tbot/
    env_file:
      - ~/.env
